<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lore Coffee</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <!-- Header -->
    <%- include('partials/nav') %> 

    <main class="container mx-auto p-6">
        <h1 class="text-3xl font-bold text-gray-800 mt-6 text-center">Edit Menu</h1>

        <hr class="my-6 border-gray-300">

        <!-- Form Edit Profil -->
        <form action="/profile/edit/<%= profiles.id %>" method="post" class="bg-white shadow-lg rounded-lg p-6 mt-6 w-11/12 max-w-md mx-auto">
            <div class="mb-4">
                <label for="userName" class="block text-gray-700 font-semibold">User Name</label>
                <input type="text" name="userName" id="userName" value="<%= profiles.User.userName %>"
                       class="w-full px-4 py-2 mt-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="mb-4">
                <label for="profileName" class="block text-gray-700 font-semibold">Profile Name</label>
                <input type="text" name="profileName" id="profileName" value="<%= profiles.profileName %>"
                       class="w-full px-4 py-2 mt-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="mb-4">
                <label for="phoneNumber" class="block text-gray-700 font-semibold">Phone Number</label>
                <input type="number" name="phoneNumber" id="phoneNumber" value="<%= profiles.phoneNumber %>"
                       class="w-full px-4 py-2 mt-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="mb-4">
                <label for="email" class="block text-gray-700 font-semibold">Email</label>
                <input type="text" name="email" id="email" value="<%= profiles.User.email %>"
                       class="w-full px-4 py-2 mt-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="form-buttons flex justify-between mt-6">
                <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-6 rounded-lg shadow-md transition duration-300">
                    Update
                </button>
                <a href="/profile" class="bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-6 rounded-lg shadow-md transition duration-300">
                    Cancel
                </a>
            </div>
        </form>
    </main>
</body>
</html>